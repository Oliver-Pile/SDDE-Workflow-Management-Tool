<h1>Projects</h1>

<div id="projects">
  <div class="container-fluid text-center">
    <div class="row">
      <% @projects.each do |project| %>
        <%= render project %>
      <% end %>
    </div>
  </div>
</div>

<%= link_to "New project", new_project_path %>
